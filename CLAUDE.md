This is the source code for the personal web site of Mathew (Mat) Lowery, a senior software engineer.

It is a fork of https://github.com/webrating/starfolio.

It will be hosted on Cloudflare Pages and accessible as https://matlowery.com.

Dev server can be run with `ASTRO_TELEMETRY_DISABLED=1 npm run dev -- --host 0.0.0.0`.




Rename ChatGPT to Codex and use an SVG for OpenAI Codex.

Icons for CoreDNS and Redis don't render well in dark mode (they're just pure white). Fix this in whatever way is necessary so that dark mode respects colors in the original svg without surrounding with white padding. Consider downloading better assets if needed.

In the About section, perhaps shorten this sentence "Over the past decade, I've worked deeply within the Kubernetes ecosystem, including container registries, K8s apiserver, etcd, operators, and CoreDNS at large scale." to "Over the past decade, I've worked deeply within the Kubernetes ecosystem at large scale." Then get rid of ": automating repeatable work, improving observability, documenting operational paths, reducing cognitive load, and building safer workflows around infrastructure change."

